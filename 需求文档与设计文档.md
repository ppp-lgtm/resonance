
---

# 第一部分：需求规格说明书 (SRS)

| 文档版本 | 修改日期 | 作者 | 备注 |
| :--- | :--- | :--- | :--- |
| V1.0 | 2026-07-23 | - | 初稿，Vue3 + Spring Boot 技术栈重构 |

## 1. 引言

### 1.1 项目名称
**Resonance** —— 全栈个人品牌数字简历系统

### 1.2 项目背景与目标
作为一名软件工程专业的学生，需要一个能够动态展示个人技能、项目经历和荣誉成就的数字平台。本项目旨在构建一个**前后端分离**的全栈Web应用，包含供访客浏览的精美前台与供管理员（本人）进行内容管理的后台。项目本身将作为核心作品，展示全栈开发能力。

### 1.3 目标用户
-   **访客**：HR、技术面试官、潜在合作伙伴。通过浏览器访问前台，了解站长的技术能力与项目经验。
-   **管理员**：站长本人。通过专用后台入口登录，管理前台展示的全部内容，实现“零代码”更新简历。

### 1.4 整体功能概述
-   **前台展示系统**：以精美、动态的界面呈现个人信息、技术栈、项目作品、奖项与教育经历。
-   **后台管理系统**：提供安全的内容管理功能，允许管理员对个人信息、技能、项目、奖项等模块进行增删改查。
-   **认证与安全**：后台接口需通过JWT（JSON Web Token）进行权限校验，保护数据安全。

## 2. 用户故事 (User Stories)

### 2.1 访客视角
-   US-01：作为访客，我希望在首页看到站长的核心信息（姓名、头衔、联系方式），以便快速了解对方。
-   US-02：作为访客，我希望可以通过技术分类查看站长的技能栈，以评估其与岗位的匹配度。
-   US-03：作为访客，我希望浏览站长的精选项目，查看项目截图、描述、技术标签和相关链接。
-   US-04：作为访客，我希望通过一个时间线清晰了解站长的教育背景和所获奖项。
-   US-05：作为访客，我希望网站无论在电脑还是手机上浏览，都有流畅、精美的视觉体验。

### 2.2 管理员视角
-   US-06：作为管理员，我希望通过账号密码安全登录后台，防止他人篡改数据。
-   US-07：作为管理员，我希望在后台能轻松更新我的基本信息、头像和联系方式。
-   US-08：作为管理员，我希望可以上传新项目，包括多张截图、GitHub地址和在线演示链接。
-   US-09：作为管理员，我希望可以上传获奖证书的图片，并能按时间排序展示。
-   US-10：作为管理员，我希望删除某个项目或奖项时，系统会进行二次确认，防止误操作。

## 3. 功能性需求

### 3.1 前台功能模块

#### M1：全局导航
-   **FR-01**：顶部固定导航栏，采用半透明毛玻璃效果，显示Logo和导航菜单。
-   **FR-02**：点击导航菜单项，页面平滑滚动至对应模块。
-   **FR-03**：支持“亮色/暗色”主题一键切换，偏好应保存在本地。

#### M2：Hero 区
-   **FR-04**：动态展示管理后台设置的头像、姓名、核心头衔、个人Slogan。
-   **FR-05**：头衔文字应有循环打字机动画效果。
-   **FR-06**：提供“查看项目”和“下载简历”两个CTA按钮，简历PDF文件由后台动态配置。

#### M3：关于我
-   **FR-07**：展示后台设置的个人介绍。
-   **FR-08**：以卡片形式展示联系方式（邮箱、GitHub、LinkedIn、个人网站等），点击邮箱可复制，其他可跳转。

#### M4：技术栈
-   **FR-09**：按“前端”、“后端”、“数据库”等类别分组展示技能。
-   **FR-10**：每个技能以图标+名称+熟练度的形式呈现。
-   **FR-11**：技能卡片悬停时有彩色高亮和上浮效果。

#### M5：项目作品
-   **FR-12**：以瀑布流或网格布局展示项目卡片，含封面图、标题、简介、技术标签。
-   **FR-13**：点击卡片，在模态框中查看项目详情，内含图片轮播（或视频嵌入）、完整描述、GitHub和演示链接。
-   **FR-14**：支持按技术标签筛选项目。

#### M6：荣誉与教育
-   **FR-15**：以垂直时间线形式展现奖项与教育经历。
-   **FR-16**：时间线节点有脉冲动画，奖项证书图片可点击放大查看。

### 3.2 后台功能模块

#### M7：管理员认证
-   **FR-17**：提供独立的后台登录页面。
-   **FR-18**：登录成功签发JWT Token，并设置过期时间（如7天）。所有后端管理API需在请求头携带Token。
-   **FR-19**：Token过期或无效时，自动重定向至登录页。

#### M8：仪表盘
-   **FR-20**：登录后首页，以数据卡片形式展示项目总数、奖项总数等统计信息。

#### M9：个人信息管理
-   **FR-21**：支持编辑姓名、头衔、Slogan、个人介绍（Markdown编辑器）。
-   **FR-22**：支持上传头像（含裁剪功能）和简历PDF。
-   **FR-23**：支持增删改联系方式条目。

#### M10：技能管理
-   **FR-24**：以表格形式展示所有技能，支持按分类筛选。
-   **FR-25**：支持新增、编辑、删除技能，可设置分类、名称、图标、熟练度。

#### M11：项目管理
-   **FR-26**：以列表形式展示项目，显示缩略图、标题、创建时间。
-   **FR-27**：新增/编辑项目表单：标题、简介、详细描述（Markdown编辑器）、技术标签、GitHub链接、演示链接。
-   **FR-28**：支持上传多张项目截图，并支持拖拽排序。
-   **FR-29**：支持嵌入视频链接（YouTube/B站），后台自动解析为预览。
-   **FR-30**：删除操作需二次弹窗确认。

#### M12：奖项与教育管理
-   **FR-31**：支持新增、编辑、删除奖项，包含名称、颁发机构、获得时间、证书图片上传。
-   **FR-32**：支持新增、编辑、删除教育经历，包含学校、学位、专业、起止时间。

## 4. 非功能性需求

### 4.1 性能
-   NFR-01：前台首页加载时间（LCP） < 2.5秒。
-   NFR-02：所有API接口响应时间95%分位 < 500ms。
-   NFR-03：图片资源应进行压缩和懒加载。

### 4.2 安全性
-   NFR-04：所有后台API必须进行JWT鉴权，无效Token返回401。
-   NFR-05：密码使用BCrypt强哈希算法加密存储，永不落库明文。
-   NFR-06：前端应对所有用户输入进行校验，后端需进行二次校验。
-   NFR-07：文件上传需限制类型（jpg，png，gif，webp，svg，pdf）和大小（≤10MB）。

### 4.3 可用性与可维护性
-   NFR-08：UI设计需遵循“精美、独特、简约、大气”原则，有统一的色彩和动效规范。
-   NFR-09：代码结构清晰，关键逻辑有注释。
-   NFR-10：前后端项目提供独立的`README.md`，说明如何本地运行和部署。

---

# 第二部分：软件设计说明书 (SDD)

| 文档版本 | 修改日期 | 作者 | 备注 |
| :--- | :--- | :--- | :--- |
| V1.0 | 2026-07-23 | - | 初稿 |

## 1. 架构设计

系统采用**前后端分离**的B/S架构，前端Vue 3应用与后端Spring Boot应用通过HTTP/HTTPS协议进行RESTful API通信。

```
[客户端浏览器]
    │
    ├──→ [Nginx 反向代理服务器]  ← 公网入口，处理HTTPS、静态资源与API转发
    │         │
    │         ├──→ /api/* (动态请求)  →  [Spring Boot 后端服务]  →  [PostgreSQL 数据库]
    │         │                                    ↑
    │         │                              文件上传/下载
    │         │                                    ↓
    │         └──→ / (静态资源)     →  [Vue 3 前端静态文件]  [阿里云OSS/MinIO 对象存储]
    │
    └─────────────────────────────────────────────────────────
```

**部署环境**：一台公网云服务器（推荐阿里云ECS或腾讯云CVM，2核4G起步），操作系统 CentOS 7.9+ 或 Ubuntu 20.04+。

## 2. 技术栈选型

| 层级 | 技术 | 版本 | 选型理由 |
| :--- | :--- | :--- | :--- |
| **前端框架** | Vue | 3.4+ | 组合式API(Composition API)、`<script setup>`语法糖，开发体验优秀。 |
| **构建工具** | Vite | 5+ | 极速冷启动和HMR，Vue官方推荐。 |
| **前端语言** | TypeScript | 5.0+ | 为Vue3提供完善的类型支持，减少运行时错误。 |
| **UI组件库** | 自定义 + **PrimeVue** / **Naive UI** (配合Tailwind) | - | 以Tailwind CSS进行原子化样式定制，PrimeVue或Naive UI提供无样式或可深度定制的基础组件（模态框、通知等）。 |
| **样式方案** | Tailwind CSS | 3.4+ | 实现“精美独特”设计的利器，快速构建一致的色彩和间距体系。 |
| **动画库** | GSAP / Motion One | - | 高性能，可处理滚动触发动画、时间线动画，实现“大气”的视觉感受。 |
| **后端框架** | Spring Boot | 3.2.x | 企业级后端事实标准，生态丰富，国内就职首选。 |
| **认证授权** | Spring Security + JWT | - | 成熟的安全框架，结合JWT实现无状态认证。 |
| **数据访问** | Spring Data JPA + QueryDSL | - | JPA实现基础CRUD，QueryDSL处理复杂动态查询。 |
| **数据库** | PostgreSQL | 16+ | 功能强大的开源关系型数据库，对JSON等类型支持良好。 |
| **对象存储** | MinIO (自建) 或 阿里云OSS | - | 自建MinIO成本低，数据可控；OSS稳定且有CDN。此处选择**MinIO**以降低初期成本。 |
| **前端部署** | Nginx | 1.22+ | 高并发反向代理服务器，直接托管Vue静态文件。 |
| **后端部署** | Docker & Docker Compose | - | 容器化部署，统一环境，方便在公网服务器上一键拉起。 |

## 3. 前端设计

### 3.1 项目结构
```
resonance-frontend/
├── public/
├── src/
│   ├── api/              # 封装所有后端API调用（axios实例）
│   ├── assets/           # 静态资源（图片、字体、全局样式）
│   ├── components/       # 全局通用组件
│   │   ├── common/       # 按钮、输入框等原子组件
│   │   └── layout/       # 导航栏、页脚等布局组件
│   ├── composables/      # 组合式函数（复用逻辑）
│   ├── router/           # Vue Router 路由配置
│   ├── stores/           # Pinia 状态管理（用户主题、后台token）
│   ├── types/            # TypeScript 类型定义
│   ├── utils/            # 工具函数
│   └── views/            # 页面级组件
│       ├── front/        # 前台页面
│       └── admin/        # 后台管理页面
├── tailwind.config.js
└── vite.config.ts
```

### 3.2 组件树 (部分)
-   `App.vue`
    -   `FrontLayout.vue`
        -   `NavBar.vue` (导航、主题切换)
        -   `HeroSection.vue` (头像、打字机效果)
        -   `AboutSection.vue`
        -   `SkillSection.vue` → `SkillCard.vue`
        -   `ProjectSection.vue` → `ProjectCard.vue` (网格视图)
        -   `ProjectModal.vue` (详情弹窗，含图片轮播)
        -   `TimelineSection.vue` → `TimelineItem.vue`
        -   `FooterSection.vue`
    -   `AdminLayout.vue`
        -   `AdminSidebar.vue`
        -   `AdminLogin.vue`
        -   `DashboardView.vue`
        -   `ProjectManage.vue` → `ProjectFormModal.vue`
        -   `SkillManage.vue`
        -   …

### 3.3 状态管理 (Pinia)
-   `useThemeStore`: 管理 'light' / 'dark' 主题状态，联动 `localStorage`。
-   `useAuthStore`: 存储管理员 JWT Token、登录状态、登出逻辑。
-   `useAppStore`: 存储前端展示的数据（个人信息、项目列表等），供各组件共享。

### 3.4 路由设计
| 路径 | 页面 | 鉴权 |
| :--- | :--- | :--- |
| `/` | 前台首页 | 无 |
| `/admin/login` | 后台登录页 | 无 |
| `/admin` | 后台仪表盘 | 需登录 |
| `/admin/projects` | 项目管理 | 需登录 |
| `/admin/projects/new` | 新增/编辑项目 | 需登录 |
| `/admin/skills` | 技能管理 | 需登录 |
| `/admin/awards` | 奖项管理 | 需登录 |
| `/admin/settings` | 个人信息设置 | 需登录 |

路由守卫：在 `router/index.ts` 中使用 `beforeEach` 钩子，检查 `useAuthStore` 中的 `token`，若访问 `/admin` 路径但无 token，则重定向到登录页。

### 3.5 UI/UX 设计规范

-   **色彩**:
    -   **浅色模式**: 背景 `#FFFFFF`，卡片 `#F8FAFC`，主文本 `#0F172A`，强调色 `#7C3AED`。
    -   **深色模式**: 背景 `#0B1121`，卡片 `#1E293B`，主文本 `#F1F5F9`，强调色 `#A78BFA`。
-   **字体**: 标题用 `Inter` 或 `Satoshi`，正文用系统默认中文字体，代码/标签用 `JetBrains Mono`。
-   **动效**:
    -   **滚动动画**: 使用 GSAP ScrollTrigger，元素从下方淡入上移。
    -   **悬停效果**: 卡片整体上浮 `-8px`，阴影加深，过渡时间 `0.4s cubic-bezier`。
    -   **页面切换**: Vue Router 的 `<transition>` 配合淡入淡出。

---

## 4. 后端设计

### 4.1 项目结构与分层架构
```
resonance-backend/
├── src/main/java/com/yourname/resonance/
│   ├── ResonanceApplication.java
│   ├── config/                    # Spring Security、CORS、MinIO配置
│   ├── common/                    # 统一响应类、异常处理、工具类
│   ├── modules/                   # 按功能模块分包
│   │   ├── auth/                  # 认证模块
│   │   │   ├── controller/        # AuthController
│   │   │   ├── service/           # AuthService
│   │   │   ├── dto/               # LoginRequest, JwtResponse
│   │   │   └── security/          # JWT工具类、过滤器
│   │   ├── profile/               # 个人信息模块
│   │   ├── project/               # 项目模块
│   │   ├── skill/                 # 技能模块
│   │   └── award/                 # 奖项模块
│   └── infra/                     # 基础设施
│       └── storage/               # MinIO文件服务
├── src/main/resources/
│   ├── application.yml            # 数据库、MinIO等配置
│   └── application-prod.yml       # 生产环境配置
└── Dockerfile
```

**分层职责**:
-   **Controller**: 接收HTTP请求，校验入参，调用Service。
-   **Service**: 实现核心业务逻辑，处理事务。
-   **Repository**: 数据访问层，与数据库交互。
-   **Domain/Entity**: 映射数据库表的实体类。
-   **DTO**: 定义前后端数据传输的对象。

### 4.2 数据库设计 (ER图核心表)

-   **admin_user**: `id`, `username`, `password_hash`, `avatar_url`, `created_at`
-   **contact_info**: `id`, `admin_id` (外键), `platform`（如’email‘）, `value`, `link`, `is_visible`
-   **skill**: `id`, `admin_id`, `name`, `category`, `icon`, `proficiency`, `sort_order`
-   **project**: `id`, `admin_id`, `title`, `summary`, `description_md`, `github_url`, `demo_url`, `sort_order`, `is_featured`, `created_at`
-   **project_image**: `id`, `project_id` (外键), `url`, `alt_text`, `sort_order`
-   **award**: `id`, `admin_id`, `title`, `issuer`, `date`, `description`, `certificate_url`
-   **education**: `id`, `admin_id`, `school`, `degree`, `major`, `start_date`, `end_date`, `description`

*注：`admin_id` 用于支持未来多用户，当前系统固定为1个管理员。*

### 4.3 API接口设计 (RESTful)

**认证模块**
-   `POST /api/auth/login` → 登录，返回JWT Token

**个人信息模块**
-   `GET /api/profile` → 获取前台展示所需全部数据（或拆分为多个接口）
-   `PUT /api/admin/profile` → 更新个人信息（需鉴权）

**项目模块**
-   `GET /api/public/projects` → 前台获取项目列表
-   `GET /api/public/projects/{id}` → 前台获取项目详情
-   `GET /api/admin/projects` → 后台获取项目列表
-   `POST /api/admin/projects` → 新增项目
-   `PUT /api/admin/projects/{id}` → 编辑项目
-   `DELETE /api/admin/projects/{id}` → 删除项目

**文件上传**
-   `POST /api/admin/upload/image` → 上传图片，返回可访问的URL

### 4.4 安全设计
1.  **认证流程**: 用户POST `/api/auth/login` 提交用户名密码。服务端验证后，生成包含`ROLE_ADMIN`的JWT Token并返回。前端将Token存入Pinia和`localStorage`。
2.  **请求鉴权**: 前端每次请求后台API时，在`Authorization`头中携带`Bearer <token>`。
3.  **后端校验**: `JwtAuthenticationFilter` 拦截所有 `/api/admin/**` 请求，解析并验证Token有效性。Spring Security配置中，对 `/api/admin/**` 要求 `hasRole(‘ADMIN’)`。

### 4.5 统一响应格式
所有API返回如下格式的JSON：
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

---

## 5. 部署方案

### 5.1 生产环境架构
公网服务器内部署Docker环境，通过`docker-compose.yml`编排以下服务：
1.  **Nginx 容器**:
    -   监听80/443端口，处理HTTPS证书。
    -   将 `/api/` 的请求反向代理到 `spring-boot-app:8080`。
    -   将其余请求指向 `vue-frontend` 的静态文件。
2.  **Spring Boot 容器**: 运行后端JAR包。
3.  **PostgreSQL 容器**: 数据持久化，数据卷挂载到宿主机。
4.  **MinIO 容器**: 对象存储，图片等文件保存在挂载的宿主机目录。

### 5.2 CI/CD 流程 (简易版)
-   本地开发分支 (`dev`) 的功能完成后，合并到 `main` 分支。
-   在服务器上拉取 `main` 分支最新代码。
-   执行 `docker-compose up -d --build` 重新构建并启动后端和前端容器，实现一键更新。


