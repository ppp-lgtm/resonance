# 共鸣 Resonance · 功能介绍手册

> 从访客第一眼看到的首屏，到后台每一个字段的编辑入口 — 这里详细说明系统的**每一项功能能做什么、怎么用、适用于什么场景**。

---

## 目录

- [一、产品简介：它能帮你做什么](#一产品简介它能帮你做什么)
- [二、前台展示站：访客看到的样子](#二前台展示站访客看到的样子)
  - [2.1 Hero 首屏](#21-hero-首屏)
  - [2.2 关于我 About](#22-关于我-about)
  - [2.3 技能矩阵 Skills](#23-技能矩阵-skills)
  - [2.4 案例档案与研究 Projects](#24-案例档案与研究-projects)
  - [2.5 时间线 Timeline](#25-时间线-timeline)
  - [2.6 页脚联系 Footer](#26-页脚联系-footer)
- [三、后台管理系统：你操作的地方](#三后台管理系统你操作的地方)
  - [3.1 登录与注册引导](#31-登录与注册引导)
  - [3.2 仪表盘 Dashboard](#32-仪表盘-dashboard)
  - [3.3 个人信息 Profile](#33-个人信息-profile)
  - [3.4 技能管理 Skills](#34-技能管理-skills)
  - [3.5 项目管理 Projects](#35-项目管理-projects)
  - [3.6 奖项与教育 Awards & Education](#36-奖项与教育-awards--education)
- [四、设计与交互特色亮点](#四设计与交互特色亮点)
- [五、新手上路：5 分钟搭好你的作品集](#五新手上路5-分钟搭好你的作品集)
- [六、功能清单速查表](#六功能清单速查表)

---

## 一、产品简介：它能帮你做什么

**共鸣 Resonance** 是一套**个人品牌一站式解决方案**，解决以下痛点：

| 痛点 | 共鸣的解法 |
| :-- | :-- |
| 想做个人作品集但不会写前端代码 | 开箱即用的杂志级排版前台，录入内容即呈现 |
| 想自己掌控内容但不想折腾 CMS | 独立的后台管理系统，表单化编辑每一个字段 |
| 简历 PDF 永远滞后于真实经历 | 在线简历 + 可下载 PDF 双通道，实时同步 |
| 投递 HR 时链接显得不专业 | 自定义域名 + 品牌化视觉，一眼区别于模板站 |
| 换工作时要手动整理项目经历 | 项目数据结构化存储，随时增删改、一键上架/下架 |

### 适用人群

- 设计师 / 产品经理 / 独立开发者 / 咨询顾问等需要展示作品集的专业人士
- 希望拥有个人官网 + 后台 CMS 的全栈/非全栈从业者
- 对排版质感有要求、不想用千篇一律简历模板的人

---

## 二、前台展示站：访客看到的样子

前台地址（开发）：`http://localhost:5173`
所有内容均来自后台录入，**零硬编码**。

### 2.1 Hero 首屏

**位置**：页面打开后第一眼看到的区域
**对应后台模块**：「个人信息」→ 基本资料 + 头像 + 简历文件

| 视觉元素 | 数据来源（后台字段） | 展示效果 |
| :-- | :-- | :-- |
| 左侧大号姓名 | `name` 姓名 | DM Serif Display 衬线字体，3–4rem 字号 |
| 姓名下方头衔轮播 | `titles[]` 头衔数组（JSON） | 逐行淡入切换，最多 4 条 |
| Slogan 标语 | `slogan` | Lora 斜体，一句核心定位 |
| 右侧头像圆框 | `avatarUrl` | 圆形裁切，直径约 180px |
| 「下载简历」按钮 | `resumeUrl` | 点击直接下载 PDF/图片，支持中文文件名 |
| 快捷入口 | 自动生成 | 锚点跳转至 About / Projects / Contact |

> 💡 **使用建议**：头衔数组建议填写 2–4 个身份（如 `["产品设计师", "独立开发者", "品牌咨询顾问"]`），轮播循环播放制造动态感。

---

### 2.2 关于我 About

**位置**：Hero 向下滚动第一屏
**对应后台模块**：「个人信息」→ Bio + 从业年限 + 所在城市 + 专注方向/服务模块 + 合作模式

此模块分为左右两组：

#### 左侧 · 个人简介块
| 元素 | 数据字段 | 说明 |
| :-- | :-- | :-- |
| 段落标题 | 固定文案「— 关于我」 | 1px 分割线 + footnote 小字 |
| Bio 长文 | `bio`（MEDIUMTEXT） | 支持换行，`whitespace-pre-line` 保留格式；建议 150–300 字 |
| 元信息行 | `yearsExperience` + `location` + `workingMode` | 以 `·` 中圆点串联展示 |

#### 右侧 · 专注方向 / 服务模块 4 卡片
| 元素 | 数据字段 | 说明 |
| :-- | :-- | :-- |
| 4 张卡片 | `focusAreas[]`（JSON 数组） | 按录入顺序排列，自动生成序号 01/02/03/04 |
| 卡片标题 | `focusAreas[i]` | 例如「服务设计」「产品工程」「团队协作」「持续学习」 |
| 卡片正文 | 自动省略 | 支持后期扩展详细描述字段 |

> 💡 **使用建议**：`focusAreas` 正好填 4 项视觉最佳。如果少于 4 项，右侧会留白；多于 4 项会溢出卡片容器 —— 请在后台控制数量。

---

### 2.3 技能矩阵 Skills

**位置**：About 下方
**对应后台模块**：「技能管理」全量数据

#### 展示逻辑
1. 所有 `is_visible = true` 的技能按 `category`（分类）自动分组
2. 分类内部按 `sort_order` 升序 + `id` 升序排列
3. 每个技能条：
   - 左侧：技能名 `name`
   - 右侧：熟练度百分比 `proficiency`（tabular 等宽数字）
   - 背景：1px 进度条，宽度 = 熟练度%
4. 分类之间以 1px 水平分割线隔开

#### 常见分类示例
| 分类名 | 推荐技能 |
| :-- | :-- |
| `设计` | Figma / Sketch / Photoshop / 信息架构 / 原型设计 |
| `前端` | Vue 3 / React / TypeScript / Tailwind CSS / Vite |
| `后端` | Spring Boot / Node.js / MySQL / Redis / JPA |
| `工具与协作` | Git / Jira / Notion / Linear / 飞书 |

> 💡 **熟练度填充建议**：80+ = 精通可带队；60–79 = 熟练独立开发；40–59 = 能上手干活；<40 = 了解阶段（建议此类不勾选「前台显示」）。

---

### 2.4 案例档案与研究 Projects

**位置**：技能矩阵下方，整站核心区域
**对应后台模块**：「项目管理」全量数据

#### 卡片列表层
- 仅展示 `is_published = true` 的项目
- 排序：`is_featured = true` 的精选项目置顶 → 其余按 `sort_order` 升序
- 每张卡片包含：
  - 封面图（取项目 `images[0].url`，无图则显示 SVG 占位）
  - 项目名 `title`
  - 摘要 `summary`
  - 标签前 3 个 `tags[0..2]`
  - 状态徽标 `status`（已上线 = 绿色 · 筹备中 = 橙色 · 规划中 = 灰色）
  - 完成年份 `completionYear`

#### 详情弹窗层（点击卡片触发）
点击卡片后弹出 12 栏 Swiss Grid 详情弹窗，包含：

| 区域 | 内容 | 数据字段 |
| :-- | :-- | :-- |
| 顶栏 | 项目序号 `0N / 总项目数` + 关闭按钮 | 自动计算 |
| 左栏 2/12 | 「— 详细描述」小标题 + 状态+年份行 + 标签列表 | `status`、`completionYear`、`tags[]` |
| 中栏 10/12 | **italic 衬线大段正文**（核心视觉） | `description`（MEDIUMTEXT），支持换行 |
| 多图区 12/12 | 横向滚动图片带，支持 alt 文本无障碍 | `images[]` 按 sort_order 排序 |
| 底部链接行 12/12 | GitHub / Demo / 视频 三个图标按钮（非空才显示） | `githubUrl` / `demoUrl` / `videoUrl` |

> 💡 **性能优化设计**：为避免首屏加载所有项目的长文本与图片，列表接口仅返回摘要字段；弹窗打开时先用列表数据**兜底显示**，同时异步调用 `/api/public/projects/{id}` 拉取完整 description + images，加载完成后平滑替换。

---

### 2.5 时间线 Timeline

**位置**：项目下方
**对应后台模块**：「奖项与教育」→ 教育经历 + 获奖荣誉

此模块由两列并排组成：

#### 左列 · 教育经历
- 按 `start_date` 倒序排列（最新学历置顶）
- 每一项格式：
  ```
  2020 – 2024      清华大学        计算机科学与技术    学士
                   （可选描述：课程/GPA/论文...）
  ```
- `end_date` 为空时显示为「2024 – 至今」（在读状态）

#### 右列 · 奖项与荣誉
- 按 `award_date` 倒序排列
- 每一项格式：
  ```
  2024.06          红点设计奖        红点设计协会
                   （证书链接可点击 + 可选封面缩略图）
  ```

> 💡 **对比效果**：教育列偏严肃稳定（左对齐 tabular 年份），奖项列偏高光亮眼（可带封面小图 + 证书跳转）。左右视觉节奏互补。

---

### 2.6 页脚联系 Footer

**位置**：页面最底部，粘性 footer 之上
**对应后台模块**：「个人信息」→ 联系方式（ContactInfo 列表）

- 仅展示 `is_visible = true` 的联系方式
- 按 `sort_order` 升序排列
- 每个联系方式为一张独立卡片：
  - 左上：平台图标 + 平台名（`platform`）
  - 中间大号值：`value`（如邮箱地址/微信号），`copyable=true` 时点击即复制到剪贴板 + Toast 提示「已复制」
  - 右下：如果配置了 `link`，显示「打开」按钮跳转（mailto://、tel://、https:// 等）

#### 推荐录入的联系方式
| platform | value | link | copyable |
| :-- | :-- | :-- | :-- |
| Email | hello@example.com | `mailto:hello@example.com` | ✅ |
| WeChat | your_wechat_id | — | ✅ |
| Phone | +86 138-xxxx-xxxx | `tel:+86138xxxxxxxx` | ✅ |
| GitHub | @yourname | `https://github.com/yourname` | ❌（直接跳转更方便） |
| 站酷 / Behance / Dribbble | 用户名 | 主页 URL | ❌ |

---

## 三、后台管理系统：你操作的地方

后台地址（开发）：`http://localhost:5174`
所有操作**实时保存**到数据库，前台立即生效。

### 3.1 登录与注册引导

#### 首次启动流程
1. 打开后台 → 自动请求 `/api/auth/status`
2. 若 `hasAdmin = false`（管理员表为空）→ **自动跳转注册页**
3. 注册页填写：
   - 用户名 `username`（4–32 字符，唯一）
   - 密码 `password`（8–64 字符，BCrypt 加密存储）
   - 显示名称 `displayName`（可选）
4. 注册成功 → 自动签发 7 天 JWT → 跳转仪表盘
5. **之后再访问注册页会被自动重定向回登录页**（已存在管理员不允许重复注册）

#### 已初始化用户
- 直接访问登录页 → 输入用户名密码 → JWT 存入 `localStorage['admin_token']`
- 路由守卫：所有 `/` 下页面检查 token，缺失即跳登录

---

### 3.2 仪表盘 Dashboard

**登录后默认落地页**，展示当前内容录入进度：

| 统计卡片 | 数据来源 | 说明 |
| :-- | :-- | :-- |
| 项目总数 | `COUNT(project) WHERE admin_id=?` | 含未发布/已发布 |
| 精选项目数 | `COUNT WHERE is_featured=true` | 前台置顶展示的那些 |
| 技能总数 | `COUNT(skill)` | 含已隐藏的 |
| 可见技能数 | `COUNT WHERE is_visible=true` | 前台实际展示数 |
| 奖项数量 | `COUNT(award)` | — |
| 教育经历数 | `COUNT(education)` | — |
| 联系方式数 | `COUNT(contact_info WHERE is_visible=true)` | — |

仪表盘同时提供**快捷入口按钮**：跳转至「编辑个人资料」「新建项目」「添加技能」。

---

### 3.3 个人信息 Profile

**最核心的后台页面**，决定前台 Hero + About + Footer 的所有内容。

#### 基本资料卡片
| 字段名 | 输入控件 | 长度限制 | 说明 |
| :-- | :-- | :-- | :-- |
| 姓名 | 文本输入 | 64 字 | 前台 Hero 大号显示 |
| 头衔 | 文本输入，逗号/顿号分隔 | 每条 32 字 | 支持中英文逗号、顿号、空格、换行切分；**失焦后自动变成标签块** |
| Slogan 标语 | 文本输入 | 200 字 | 一句话定位 |
| 个人简介 Bio | 多行文本域 | MEDIUMTEXT（约 16MB） | 支持换行，前台原样保留排版 |
| 所在城市 | 文本输入 | 128 字 | 例：杭州 / 上海 · 远程可 |
| 从业年限 | 数字输入 | 0–99 | 例：6 |
| 合作模式 | 文本输入 | 128 字 | 例：远程 / 驻场 / 咨询 |

#### 头像与简历
| 字段 | 控件 | 说明 |
| :-- | :-- | :-- |
| 头像 | 文件上传组件 | 推荐正方形 1:1，>400×400px；支持 JPG/PNG/WebP；**点击即可更换** |
| 简历文件 | 文件上传组件 | 支持 PDF / PNG / JPG；大小 ≤10MB；上传后前台立即可下载 |

#### 专注方向 / 服务模块（重点说明）
- 输入方式：**单个多行文本域**，以中英文逗号 `,` `，`、顿号 `、`、空格、换行、分号、竖线、斜杠 作为分隔符
- **修复记录**：旧版存在「无法输入逗号」的 bug（computed setter 每敲一次键立即 split）；现改为「失焦（blur）/保存时才解析」，输入过程中逗号完全保留
- 实时预览：下方标签块显示当前解析结果，方便你确认分隔是否正确

#### 联系方式列表（ContactInfo 动态增删）
- 点击「+ 添加联系方式」弹出一行表单：
  - 平台 `platform`：Email / WeChat / Phone / GitHub / 自定义
  - 图标标识 `icon`：预留字段，前台可扩展 SVG 映射
  - 内容值 `value`：具体邮箱/微信号/手机号
  - 超链接 `link`：可为空，填写则显示跳转按钮
  - 可复制 `copyable`：默认勾选，前台点击值即复制
  - 前台显示 `is_visible`：默认勾选
  - 排序 `sort_order`：数字越小越靠前
- 支持上移/下移/删除任意一项

---

### 3.4 技能管理 Skills

**列表 + 弹窗编辑**模式，所见即所得。

#### 主列表
- 按「分类 → 排序号」分组展示
- 每行显示：分类标签、名称、熟练度进度条、显示/隐藏开关、操作按钮
- 顶部「+ 新增技能」打开弹窗

#### 新增/编辑弹窗字段
| 字段 | 控件 | 说明 |
| :-- | :-- | :-- |
| 技能名 | 文本输入，64 字 | **同管理员下唯一**（数据库唯一约束），重名保存会报错 |
| 分类 | 下拉选择 or 手动输入 | 不存在的分类会自动创建（无需提前维护分类表） |
| 图标标识 | 可选输入 | 预留字段，后续可接入 SVG Sprite |
| 熟练度 | 滑块 + 数字输入（0–100） | 实时同步 |
| 排序号 | 数字输入 | 同分类内越小越靠前；留空默认 0 |
| 前台显示 | 开关（默认开） | 关闭后前台不可见，录入未掌握技能时用 |

> 💡 **批量录入技巧**：先录入同一分类的所有技能（都填相同分类名），再统一调整每个分类内部的 sort_order，比一条一条切换效率高。

---

### 3.5 项目管理 Projects

**内容最丰富的后台模块**，支持完整 CRUD + 多图管理。

#### 项目列表页
- 顶部工具条：
  - 搜索框：按 `title` / `summary` / `tags` 模糊搜索
  - 标签筛选：下拉选中某标签，仅展示含该标签的项目
  - 「+ 新建项目」按钮
- 表格列：
  - 封面缩略图
  - 项目名称 + 摘要（截断 1 行）
  - 状态标签（已上线/筹备中/规划中，彩色 badge）
  - 完成年份
  - 精选开关 ★
  - 发布开关 ✓
  - 排序号
  - 操作：编辑 / 删除

#### 新建/编辑弹窗（12 栏表单）
顶部 2 个开关独立行：
- ★ 设为精选（置顶）
- ✓ 发布到前台

主体表单字段：

| 区块 | 字段 | 控件 | 说明 |
| :-- | :-- | :-- | :-- |
| 基础信息 | 项目名称 | 文本输入，200 字 | 必填 |
| 基础信息 | 卡片摘要 | 文本输入，500 字 | 必填；前台卡片上显示的那一句话 |
| 基础信息 | 标签 | 文本域，逗号/顿号分隔 | 同 focusAreas 机制，失焦后解析为标签块 |
| 基础信息 | 项目状态 | 单选下拉 | **已上线 / 筹备中 / 规划中** 三选一 |
| 基础信息 | 完成年份 | 数字输入（4 位） | 例：2026；可为空（在研项目） |
| 内容区 | 详细描述 | 多行文本域，MEDIUMTEXT | 必填；前台弹窗 italic 大段正文，支持换行排版 |
| 外链区 | GitHub 仓库 | URL 输入 | 非空则前台显示 GitHub 按钮 |
| 外链区 | 演示站链接 | URL 输入 | 非空则前台显示 Demo 按钮 |
| 外链区 | 视频链接 | URL 输入 | B站/YouTube 等 |
| 媒体区 | 项目多图 | 拖拽上传组件 | 支持多张上传（JPG/PNG/WebP ≤10MB/张）；支持拖拽排序；支持 alt 文本编辑；删除即同步数据库（orphanRemoval） |
| 排序 | 排序号 | 数字 | 默认 0，越小越靠前 |

> 💡 **最佳实践**：`description` 与 `summary` 职责分离 — summary 是 1 句话钩子（吸引点击），description 是完整叙述（点击后阅读）。前者 ≤50 字效果最佳。

---

### 3.6 奖项与教育 Awards & Education

在同一页面中分为左右两个 Tab，操作模型一致（列表 + 弹窗编辑）。

#### 奖项 Tab
| 字段 | 控件 | 必填 | 说明 |
| :-- | :-- | :-- | :-- |
| 奖项名称 | 文本，255 字 | ✅ | 例：「IF 设计奖 · 产品设计类」 |
| 颁发机构 | 文本，255 字 | ✅ | 例：汉诺威工业设计论坛 |
| 获奖日期 | 日期选择器 | ✅ | 前台格式化为 `YYYY.MM` |
| 奖项描述 | 多行文本域 | ❌ | 可为空；获奖理由/作品介绍 |
| 证书文件 | 文件上传 | ❌ | PDF/图片，前台显示证书链接按钮 |
| 封面图 | 图片上传 | ❌ | 奖杯/奖状缩略图，前台展示在时间线旁 |
| 排序号 | 数字 | — | 默认按日期倒序，同日期时 sort_order 生效 |

#### 教育 Tab
| 字段 | 控件 | 必填 | 说明 |
| :-- | :-- | :-- | :-- |
| 学校名称 | 文本，255 字 | ✅ | — |
| 学位 | 下拉选择 | ✅ | 学士 / 硕士 / 博士 / 专科 / 其他 |
| 专业 | 文本，255 字 | ✅ | 例：交互设计 / 计算机科学与技术 |
| 入学日期 | 日期选择器 | ✅ | — |
| 毕业日期 | 日期选择器 | ❌ | 为空 = 在读；前台显示「至今」 |
| 描述 | 多行文本域 | ❌ | 主修课程 / GPA / 学术成果等 |

---

## 四、设计与交互特色亮点

### 🎨 视觉层
1. **12 栏 Swiss Grid**：所有前台页面严格遵循 12 栏网格 + 8px 基数间距，保证排版呼吸感
2. **三色系配色**：Paper White `#F4F1EC`（纸白背景）+ Cinnabar Red `#B23A2E`（朱红强调）+ Forest Green `#2F4538`（墨绿辅色），**全站不使用任何渐变**
3. **衬线+无衬线混排**：
   - 标题与项目详细描述：DM Serif Display（衬线粗体）+ Lora Italic（衬线斜体）
   - 正文与表单：DM Sans（几何无衬线）
   - 数字与代码：JetBrains Mono（等宽，tabular-nums 保证数字对齐）
4. **SVG 纸质纹理背景**：通过内联 SVG 滤镜模拟纸张质感与轻网格，比 PNG 体积小 90% 且无损放大

### ⚡ 交互层
1. **项目详情双阶段加载**：点击卡片 → 立即用列表数据渲染弹窗（零白屏）→ 异步拉详情后平滑更新正文与图片
2. **标签输入防逗号丢失**：Profile 的 focusAreas 与 Project 的 tags 均采用「失焦再解析」策略，输入过程中逗号自由保留
3. **技能滑块实时同步**：拖拽滑块的同时进度条与数字输入框双向绑定
4. **联系方式一键复制**：copyable=true 的卡片点击后 Clipboard API 复制 + 页面 Toast

### 🔒 安全层
1. JWT 无状态鉴权，BCrypt 密码哈希（Spring Security 默认强度）
2. 管理接口统一走 `CurrentAdmin.id()` 校验，确保多管理员场景下数据隔离
3. 上传文件白名单（仅 JPG/PNG/WebP/PDF），单文件 ≤10MB，整请求 ≤50MB
4. 前台接口只返回 `is_published=true` / `is_visible=true` 的数据，草稿项目不会泄露

### 📦 工程层
1. **首屏聚合接口** `/api/public/overview`：前台原本 6 个请求合并为 1 个，TTFB 降低 60%+
2. JPA 级联操作：删除项目自动删除所有 ProjectImage（`orphanRemoval=true`）
3. Jackson 枚举序列化：ProjectStatus 枚举 JSON 直接输出「已上线」中文，前后端无需翻译映射
4. 多环境 Profile：dev=H2/MySQL+ddl-auto:update，prod=MySQL+ddl-auto:validate，开箱即可切换

---

## 五、新手上路：5 分钟搭好你的作品集

按此步骤操作，保证最快从空库 → 前台完整展示：

### 第 1 分钟：初始化管理员
- 启动后端 + 后台前端
- 打开 `http://localhost:5174` → 自动跳转注册页
- 注册账号：用户名 `admin`，密码 `你记得住的强密码`
- 自动登录进仪表盘

### 第 2 分钟：填写个人信息
- 点击侧边栏「个人信息」
- 填写：姓名、3 个头衔（逗号分隔）、Slogan、Bio（写 2–3 段）、所在城市、从业年限、合作模式
- 上传头像（正方形照片）+ 上传简历 PDF
- 「专注方向」填 4 个（如：服务设计、产品工程、团队协作、持续学习）
- 「联系方式」至少填 2 项：Email + GitHub
- **点击保存**

### 第 3 分钟：添加 3 个技能
- 侧边栏「技能管理」
- 新增 1：Vue 3，分类 前端，熟练度 85
- 新增 2：Figma，分类 设计，熟练度 80
- 新增 3：Spring Boot，分类 后端，熟练度 75
- 每个保存完即可

### 第 4 分钟：添加 1 个项目
- 侧边栏「项目管理」→ 新建项目
- 勾选「发布到前台」
- 填：名称、摘要、3 个标签、状态=已上线、完成年份=2026
- 详细描述写 3–5 行（这是点击后弹窗的核心内容）
- 上传 2–4 张项目截图（第一张自动成为卡片封面）
- 保存

### 第 5 分钟：查看前台效果
- 打开 `http://localhost:5173`
- 应该能看到：姓名+头衔轮播 → About 段 → 3 个技能进度条 → 项目卡片 → 空时间线 → 联系方式
- 点击项目卡片，验证详情弹窗、图片横滑、GitHub/Demo 按钮

**完成！** 🎉 继续回后台丰富内容即可。

---

## 六、功能清单速查表

### 前台展示功能

| 功能模块 | 子功能 | 支持 |
| :-- | :-- | :-- |
| Hero | 姓名展示 | ✅ |
| Hero | 头衔轮播（数组循环） | ✅ |
| Hero | Slogan 斜体标语 | ✅ |
| Hero | 头像圆形裁切 | ✅ |
| Hero | 简历下载（PDF/图片） | ✅ |
| Hero | 页面锚点导航 | ✅ |
| About | Bio 长文保留换行 | ✅ |
| About | 从业年限+城市+合作模式元信息行 | ✅ |
| About | 4 卡片专注方向 | ✅ |
| Skills | 按分类分组显示 | ✅ |
| Skills | 熟练度百分比进度条 | ✅ |
| Skills | 分类间水平分割线 | ✅ |
| Projects | 卡片网格（封面+摘要+标签+状态+年份） | ✅ |
| Projects | 精选置顶 + 排序号 | ✅ |
| Projects | 详情弹窗（12 栏 Grid） | ✅ |
| Projects | 详细描述 italic 衬线正文 | ✅ |
| Projects | 多图横向滚动展示 | ✅ |
| Projects | GitHub/Demo/视频外链按钮 | ✅ |
| Projects | 双阶段加载（兜底+异步刷新） | ✅ |
| Timeline | 教育经历倒序 | ✅ |
| Timeline | 在读状态显示「至今」 | ✅ |
| Timeline | 奖项倒序 + 日期格式 YYYY.MM | ✅ |
| Timeline | 奖项证书链接 + 封面图 | ✅ |
| Footer | 联系方式卡片网格 | ✅ |
| Footer | 一键复制到剪贴板 + Toast | ✅ |
| Footer | 超链接跳转（mailto/tel/https） | ✅ |

### 后台管理功能

| 功能模块 | 子功能 | 支持 |
| :-- | :-- | :-- |
| 登录 | 首次启动自动跳注册 | ✅ |
| 登录 | JWT 7 天有效期 | ✅ |
| 登录 | 路由守卫（缺 token 跳登录） | ✅ |
| 登录 | BCrypt 密码加密 | ✅ |
| 仪表盘 | 7 项统计数字 | ✅ |
| 仪表盘 | 快捷入口按钮 | ✅ |
| Profile | 基本资料 7 字段 | ✅ |
| Profile | 头像上传与替换 | ✅ |
| Profile | 简历文件上传 | ✅ |
| Profile | 头衔数组（失焦解析为标签） | ✅ |
| Profile | 专注方向/服务模块（失焦解析） | ✅ |
| Profile | 联系方式 CRUD + 排序 | ✅ |
| Skills | 技能 CRUD | ✅ |
| Skills | 分类分组 | ✅ |
| Skills | 熟练度滑块 | ✅ |
| Skills | 显示/隐藏开关 | ✅ |
| Skills | 同管理员下技能名唯一校验 | ✅ |
| Projects | 项目 CRUD | ✅ |
| Projects | 搜索（标题/摘要/标签） | ✅ |
| Projects | 标签筛选 | ✅ |
| Projects | 精选/发布开关 | ✅ |
| Projects | 状态三选一（已上线/筹备中/规划中） | ✅ |
| Projects | 完成年份手动输入 | ✅ |
| Projects | 详细描述长文本 | ✅ |
| Projects | 多图上传 + 拖拽排序 + alt 编辑 | ✅ |
| Projects | 外链三件套（GitHub/Demo/视频） | ✅ |
| Awards | 奖项 CRUD | ✅ |
| Awards | 日期选择器 + 格式化 | ✅ |
| Awards | 证书 + 封面双上传 | ✅ |
| Education | 教育经历 CRUD | ✅ |
| Education | 学位下拉（本/硕/博/专科/其他） | ✅ |
| Education | 空毕业日期 = 在读 | ✅ |
| 上传 | 单文件 ≤10MB，总请求 ≤50MB | ✅ |
| 上传 | 格式白名单（JPG/PNG/WebP/PDF） | ✅ |

---

> **共鸣 Resonance** — 不是又一个简历模板，而是一套长期维护你个人品牌的工作流。
